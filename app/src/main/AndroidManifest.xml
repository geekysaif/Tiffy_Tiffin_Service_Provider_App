<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="logic.mania.tiffy">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="logic.mania.tiffy.activity.CartActivity"></activity>
        <activity android:name="logic.mania.tiffy.activity.SignUpActivity" />
        <activity android:name="logic.mania.tiffy.activity.LoginActivity" />
        <activity android:name="logic.mania.tiffy.util.ActivityBase" />
        <activity
            android:name="logic.mania.tiffy.activity.TiffinDetailsActivity"
            android:exported="true"
            android:screenOrientation="locked" />
        <activity
            android:name="logic.mania.tiffy.activity.MainActivity"
            android:screenOrientation="locked" />
        <activity
            android:name="logic.mania.tiffy.activity.SplashActivity"
            android:screenOrientation="locked"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>